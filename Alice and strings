#include <stdio.h>
#include<string.h>
int main()
{
    char a[1000000],b[1000000];
    scanf("%s%s",a,b);
    int i,s=0,n;
    n=strlen(a);
    if(n==strlen(b))
    {
    for(i=0;a[i]!='\0';i++)
    {
        if(a[i]<=b[i])
        s++;
    }
    }
    if(s==n)
    printf("YES");
    else
    printf("NO");
}
